[project]
name = "forum-cli"
version = "0.0.1"
description = "CLI tool wrapping forum-sdk"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/forum_cli"]
